.component.more-drawer
  :if phone
    %section
      %menu
        :if !mulberry.app.UI.isInBrowser
          =helpers.homeButton
        =helpers.fontSizeButton
        =helpers.searchButton

    %section
      %menu
        =helpers.favoriteButton
        =helpers.sharingButtons

  :if tablet
    %section
      %menu
        =helpers.fontSizeButton
        =helpers.favoriteButton
        =helpers.sharingButtons

  %section{ dojoAttachPoint : 'shareSection' }
